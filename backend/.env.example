# Location of the application data directory (database, exports, clones).
# If unset, a platform-specific user data directory is used automatically.
REBUILT_DATA=../data

# Set to "yes" to run in production mode (serves the bundled frontend, disables docs).
PRODUCTION=no
